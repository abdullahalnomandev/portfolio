import React from 'react';
import Headline from '../ui/Headline';

const Skill = () => {
    return (
        <div>
            <Headline title='skills' />
        </div>
    );
};

export default Skill;